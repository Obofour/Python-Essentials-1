# are two numbers equal 
#To ask this question the ==(equal ,equal) operator is used.
# = is an assignment operator,eg,a=b assigns a=b with values of b;
# == is te question are these values equal? so a== b compares a and b.
#It is a binary operator with left-sided binding.it needs two argument a check if they are equal

